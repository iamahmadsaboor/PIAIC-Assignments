/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.ctfassets.net",
      "images.remotePatterns",
      "dummyimage.com",
    ],
  },
};

export default nextConfig;
